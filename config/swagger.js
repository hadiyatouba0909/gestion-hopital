// config/swagger.js
const swaggerJsDoc = require('swagger-jsdoc');

const swaggerOptions = {
  swaggerDefinition: {
    openapi: '3.0.0',
    info: {
      title: 'API Cabinet Médical',
      version: '1.0.0',
      description: 'API de gestion de cabinet médical',
    },
    servers: [
      {
        url: 'http://localhost:5000',
        description: 'Serveur de développement',
      },
    ],
    components: {
      securitySchemes: {
        bearerAuth: {
          type: 'http',
          scheme: 'bearer',
          bearerFormat: 'JWT',
        },
      },
    },
  },
  apis: ['./routes/*.js'], // Assurez-vous que ce chemin pointe vers les fichiers de vos routes
};


module.exports = swaggerJsDoc(swaggerOptions);